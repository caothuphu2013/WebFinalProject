<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">
            <strong>Hãng sản xuất</strong>
        </h3>
    </div>
    <div class="list-group">
        {{#each layoutVM.brandsList}}
            <div class="list-group-item click-box">
                <input type="checkbox" class="{{this.name}} checkbox checkbox-btn" checked>  
                <a href="/shop/choose?att=brand&id={{this.id}}" class="{{this.name}} content-drop-small">{{this.name}}</a>
            </div>    
        {{/each}}
    </div>
</div>